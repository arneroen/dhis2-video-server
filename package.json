{
  "name": "video-server",
  "version": "0.0.1",
  "description": "Video repository server for videos in the DHIS2 android capture app.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "arneroen",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "uuid": "^3.3.3"
  }
}
